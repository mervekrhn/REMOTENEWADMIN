// src/services/authService.js
/*const login = async (email, password) => {
  const response = await axios.post(
    `${API_URL}/login`,
    { email, password },
    { withCredentials: true }  // Cookie'lerin backend'den tarayıcıya taşınması için gerekli
  );
  return response.data;
};
*/